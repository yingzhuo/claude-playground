-- ------------------------------------------------------------------------------------------------------------------
-- 后台管理员表：username 改为唯一性索引
-- ------------------------------------------------------------------------------------------------------------------

-- @formatter:off
alter table `t_admin` drop index `idx_username`, add unique index `uk_username` (`username`);
-- @formatter:on
